[pytest]
# `pythonpath = .` puts the repo root on sys.path so tests can do
# `from src.features import ...` without an editable install. Run from the
# repo root:  python -m pytest tests/ -q
pythonpath = .
testpaths = tests
addopts = -q
